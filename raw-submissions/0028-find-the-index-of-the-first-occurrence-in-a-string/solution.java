class Solution {
    public int strStr(String haystack, String needle) {
        // StringBuilder sb = new StringBuilder(haystack);
        // for(char c : haystack.toCharArray()){
        //     if(needle.charAt(c) != haystack.charAt)
        //         return -1;
        // }
        // return true;
        return haystack.indexOf(needle);
    }
}
